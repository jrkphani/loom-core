"""Loom Core — personal knowledge fabric daemon.

Sole writer to the SQLite structured store and the Obsidian vault. Exposes an
HTTP API on localhost (default 127.0.0.1:9100) consumed by the MCP server, the
SwiftUI Loom UI, and the `loom` CLI.
"""

__version__ = "1.0.0"
